<nav class="navbar navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand">NearBuy</a>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="https://mysterious-oasis-25704.herokuapp.com/">Home</a>
        </li>
      </ul>
      <div class="d-flex">
        <% if user_signed_in? %>
          <%= link_to "Sign Out", destroy_user_session_path, method: :delete, class:"nav-link" %>
          <%= link_to "Profile", user_profile_path(:username => current_user.username), class:"nav-link" %>
        <% else %>
          <%= link_to "Sign Up", new_user_registration_path, class:"nav-link" %>
          <%= link_to "Sign In", new_user_session_path, class:"nav-link" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>

